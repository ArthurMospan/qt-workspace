# QuickTeam — План робіт (аудит логіки + повнота таск-менеджера)

> Складено за результатами аудиту кодбази. Цей файл — точка входу для нового чату.
> Виконувати завдання по черзі, зверху вниз. Кожне завдання має: **що**, **чому**, **де** (файли), **критерій готовності**, **обсяг** (S/M/L).
>
> Легенда пріоритету: 🔴 критично · 🟠 важливо · 🟡 дрібне · 🟢 фіча

---

## Продуктові рішення власника (обовʼязкові до врахування)

1. **У внутрішньому сервісі команди НЕ повинно бути клієнтів.** Спілкування з клієнтом — тільки в окремому клієнтському порталі (проєкт `qt`). Пізніше звʼяжемо через API (звʼязка проєкту, отримання матеріалів з порталу, можливо окремий чат із клієнтом прямо з задачі). **Зараз** — жодної ролі «клієнт»/viewer/guest у нашому сервісі.
2. **Таск-менеджер має бути повноцінним.** Мінімум, якого зараз бракує: **прикріплення файлів до задачі**, **підзадачі/чеклісти**, **підписники (watchers)**. Деталі — у розділі B.

---

## A. Виправлення логіки

### 🔴 L1 — Прибрати роль «клієнт» (і згадки viewer/guest) з внутрішнього сервісу
- **Чому:** клієнту не місце в командному таск-менеджері (рішення власника №1). Зараз роль «клієнт» висить залишком і суперечить сама собі: [layout.js:90](src/app/workspace/layout.js#L90) взагалі блокує вхід клієнтам і редіректить на портал, але [can.js:28-29](src/lib/utils/can.js#L28) досі дає «клієнту» право коментувати задачі (куди він і не потрапляє).
- **Де:**
  - [src/lib/utils/can.js:28-29](src/lib/utils/can.js#L28) — прибрати `'client'` зі списків `create:comment` / `edit:comment`. Ролі лишаються: `owner`, `admin`, `member`.
  - [src/app/workspace/layout.js:89-104](src/app/workspace/layout.js#L89) — гілку `isClientOnly` (редірект на портал) **лишити як захисний запобіжник** АБО прибрати, якщо роль `client` більше ніде не існує (узгодити з тим, як завершиться API-звʼязка). Рекомендація: **лишити** — безпечний fallback.
  - [src/components/ClientProjectViewer.jsx](src/components/ClientProjectViewer.jsx) — перевірити, чи використовується у внутрішньому застосунку; якщо це чисто портальний перегляд і тут не рендериться — прибрати з воркспейсу.
- **Уточнення:** «viewer»/«guest» у коді **не існують** — це лише застарілий текст у CLAUDE.md (див. L6). Прибирати нічого, крім документа.
- **Готово, коли:** у моделі прав лишились тільки owner/admin/member; клієнт ніде не згадується як роль всередині сервісу; білд чистий.
- **Обсяг:** S

### 🔴 L2 — Ввести поняття «завершальний статус» і провести його через усю аналітику
- **Чому (найбільша системна дірка):** статуси налаштовуються в Settings, але **вся аналітика/білінг/беклог/спринти вважають, що «виконано» = рівно один статус з id `'done'`** і зашивають порядок колонок. Якщо перейменувати/додати фінальний статус — усі метрики (швидкість, навантаження, % виконання, прострочені, білінг) тихо стають брехати.
- **Де (усі захардкоджені `columnId === 'done'` та зашиті списки):**
  - [AnalyticsTab.jsx:15,48-101](src/components/workspace/AnalyticsTab.jsx#L15) — `COL_ORDER` + ~10 перевірок `=== 'done'`
  - [VelocityTab.jsx:69-351](src/components/workspace/VelocityTab.jsx#L69)
  - [WorkloadTab.jsx:75-81](src/components/workspace/WorkloadTab.jsx#L75)
  - [BillingTab.jsx:662](src/components/workspace/BillingTab.jsx#L662)
  - [BacklogTab.jsx:22](src/components/workspace/BacklogTab.jsx#L22)
  - [sprints/page.js:29-30](src/app/workspace/sprints/page.js#L29)
- **Підхід:**
  1. У конфіг статусів ([useWorkflowConfig.js](src/lib/hooks/useWorkflowConfig.js)) додати ознаку завершальності — напр. поле `isDone`/`terminal` на статусі, або похідний хелпер `doneStatusIds(statuses)`. Порядок колонок брати з `statuses` (а не із зашитих масивів).
  2. Замінити всі `columnId === 'done'` на перевірку «чи статус завершальний», а `COL_ORDER`/`COLUMNS_ORDER` — на порядок зі `statuses`.
  3. У Settings додати перемикач «вважати статус завершальним» (за замовч. останній / `done`).
- **Готово, коли:** переіменування/додавання фінального статусу коректно відображається в усіх графіках; жодного зашитого `'done'` в аналітиці не лишилось.
- **Обсяг:** L

### 🟠 L3 — Спринти мають брати статуси з конфігу (не зашиті)
- **Чому:** головний борд ([AgileBoard.jsx:49](src/components/workspace/AgileBoard.jsx#L49)) читає статуси з `useWorkflowConfig`, а сторінка Спринтів — ні, тож зміни статусів там не видно.
- **Де:** [sprints/page.js:29-34,706](src/app/workspace/sprints/page.js#L29) — завести на `useWorkflowConfig().statuses`; прибрати мертвий `COLUMNS` (рядок 34) і фіктивний проп `stages={[]}` (рядок 706).
- **Готово, коли:** спринти показують ті самі статуси, що й борд; мертвий код прибрано.
- **Обсяг:** S–M *(частково закриється разом з L2)*

### 🟠 L4 — Прибрати суперечливі дефолти видимості проєкту (ризик показати клієнту зайве)
- **Чому:** [isClientProject()](src/lib/hooks/useProjects.js#L57) вважає проєкт **без поля `visibility`** як «показувати клієнту», а створення ставить `'internal'` ([page.js:507](src/app/workspace/page.js#L507)) і вкладка порталу вважає невизначене як «не показувати». Старі проєкти без поля можуть засвітитися в порталі.
- **Де:** [useProjects.js:50-58](src/lib/hooks/useProjects.js#L50) — узгодити дефолт: `undefined` → **internal** (безпечно), тобто `isClientProject` має вимагати саме `visibility === 'shared'`.
- **Готово, коли:** невизначена видимість скрізь трактується однаково (internal/не показувати клієнту).
- **Обсяг:** S · ⚠️ перевірити в браузері на реальних проєктах перед злиттям.

### 🟡 L5 — `can.js`: fallback під час завантаження ролі
- **Де:** [can.js:40-44](src/lib/utils/can.js#L40) — поки роль не завантажилась, дозволяється `create:project`, хоча звичайний `member` цього не має. Прибрати `create:project` з fallback або показувати нейтральний стан до завантаження.
- **Обсяг:** S

### 🟡 L6 — Полагодити CLAUDE.md (застарілі ролі)
- **Де:** CLAUDE.md пише «admin, editor, viewer roles». Реально: `owner/admin/member`. Виправити текст.
- **Обсяг:** S

---

## B. Повнота таск-менеджера (фічі, яких бракує)

### 🟢 F1 — Прикріплення файлів (матеріали) до задачі
- **Чому:** зараз файл можна кинути **тільки в коментар** ([IssueDetail.jsx:521-531](src/components/workspace/IssueDetail.jsx#L521)); повноцінного розділу «Вкладення» на самій задачі немає. Для таск-менеджера це базова функція.
- **Підхід:** додати на задачу першокласний блок «Вкладення» — завантаження, список, прев'ю (зобр./pdf/відео вже вміє переглядач матеріалів), видалення. Перевикористати наявні [uploadFile.js](src/lib/utils/uploadFile.js) / [fileUpload.js](src/lib/services/fileUpload.js) (є два — звести до одного) і `FileInput`/`ImageUpload` з UI-kit. Зберігати `attachments[]` на документі задачі.
- **Де:** IssueDetail (панель задачі), CreateTaskModal (опційно — кидати файли одразу при створенні), модель issue.
- **Готово, коли:** до задачі можна прикріпити/переглянути/видалити файл без коментаря; видно на картці задачі індикатор вкладень.
- **Обсяг:** M

### 🟢 F2 — Підзадачі / чеклісти
- **Чому:** у UI-kit це вже замокано ([ui-kit/page.js:1269](src/app/ui-kit/page.js#L1269) — `subtasks`), але в **реальних задачах не реалізовано**. Розбиття задачі на пункти — стандарт таск-менеджера.
- **Підхід:** визначитись між двома рівнями: (а) легкий **чекліст** (пункти з галочкою всередині задачі) і/або (б) повноцінні **підзадачі** (окремі issue з `parentId`, свій статус/виконавець). Рекомендація: почати з чеклісту (швидко, покриває 80% потреб), підзадачі — окремим етапом.
- **Де:** модель issue (`checklist[]` або `parentId`), IssueDetail, картка задачі (прогрес `3/5`).
- **Готово, коли:** у задачі можна додавати пункти, відмічати виконані, бачити прогрес.
- **Обсяг:** M (чекліст) / L (повні підзадачі)

### 🟢 F3 — Підписники (watchers)
- **Чому:** сповіщення йдуть лише виконавцю/автору/згаданим ([useNotifications.js](src/lib/hooks/useNotifications.js)). Немає способу «стежити» за чужою задачею. Часта потреба.
- **Підхід:** поле `watcherIds[]` на задачі; кнопка «Стежити»; додати watchers у розсилку сповіщень про зміни/коментарі.
- **Готово, коли:** можна підписатись на задачу й отримувати її сповіщення, не будучи виконавцем.
- **Обсяг:** M

---

## Зроблено раніше (для контексту)
- ✅ Прибрано недороблений «тип роботи» (workType) з трекінгу часу.
- ✅ Хвилі UI-kit: видалено мертвий код; бренд-кольори зведено в токени (`ink/canvas/muted/line/surface`); kit-інпути в модалках спринтів.

### Сесія аудиту логіки + повнота таск-менеджера (виконано)
- ✅ **L1** — роль «клієнт» прибрано з `can.js` (лишились owner/admin/member); мертвий `ClientProjectViewer.jsx` видалено; гілку `isClientOnly` у `layout.js` лишено як захисний fallback.
- ✅ **L2** — введено поняття «завершальний статус». Хелпери `getDoneStatusIds`/`isDoneStatus` + `doneStatusIds` у `useWorkflowConfig`. Замінено всі зашиті `=== 'done'` та `COL_ORDER`/`COLUMN_LABEL` в: AnalyticsTab, analytics/page.js, VelocityTab, WorkloadTab, BillingTab, BacklogTab, workspace/page.js (прогрес), а також у прострочених/нагадуваннях/залежностях (IssueCard, IssueDetail, TaskRow, useDeadlineReminders, useIssueLinks, DependenciesPanel, useIssues-guard). У Settings → «Статуси» додано перемикач **Завершальний** (матеріалізує `isDone` на статусах).
- ✅ **L3** — спринти беруть порядок статусів з `useWorkflowConfig().statuses`; прибрано мертві `COLUMNS`/`COLUMN_LABEL`/`COLUMNS_ORDER` та фіктивний `stages={[]}`.
- ✅ **L4** — `isClientProject` тепер вимагає саме `visibility === 'shared'` (undefined → internal, не показувати клієнту). ⚠️ helper у цьому репо не використовується — фактичну видимість перевіряти в порталі (`qt`).
- ✅ **L5** — з fallback у `can.js` прибрано `create:project` (member його не має).
- ✅ **L6** — CLAUDE.md: ролі виправлено на owner/admin/member.
- ✅ **F1** — першокласні **Вкладення** на задачі: вкладка «Вкладення» в IssueDetail (завантаження мультизавантаженням через `uploadFile`, список з прев'ю через наявний лайтбокс, видалення), `attachments[]` на документі задачі, індикатор 📎 на картці.
- ✅ **F2** — чекліст/підзавдання **вже були реалізовані** в IssueDetail (додати/відмітити/редагувати/видалити + прогрес + індикатор на картці). Аудит був застарілий. Розширено лише guard «не закривати з відкритими підзавданнями» → тепер поважає кастомні завершальні статуси.
- ✅ **F3** — підписники: поле `watcherIds[]`, кнопка «Стежити»/«Стежите» + аватари спостерігачів у панелі задачі; watchers додано в розсилку сповіщень про коментарі та зміну статусу.

> ℹ️ **Firestore rules:** нові поля `attachments[]` і `watcherIds[]` пишуться в документ issue. Перевірено — `firestore.rules` (match /issues) дозволяє оновлення будь-яких полів будь-якому члену орг (немає field-allowlist), тож окремих змін правил не потрібно.

## Рекомендований порядок виконання
1. **L1** (прибрати клієнта) — швидко, прибирає концептуальний шум.
2. **L2** (завершальний статус) — найбільша й найважливіша; від неї залежить коректність усієї аналітики. Разом з нею закрити **L3**.
3. **L4** (видимість проєкту) — безпека даних клієнта.
4. **F1 → F2 → F3** — повнота таск-менеджера.
5. **L5, L6** — дрібниці наприкінці.

## Перевірено — консистентно, чіпати не треба
Сповіщення (типи/канали/тумблери), позиції↔ставки↔білінг, лейбли, оцінка часу (год→хв), джерело статусів/типів/пріоритетів (`useWorkflowConfig`).

---

# Сесія 2026-07-17 — зроблено і що далі

## ✅ Зроблено в цій сесії
- **Статуси**: три різні захардкоджені списки дефолтних статусів (7 шт. у `useWorkflowConfig`, 3 шт. локально в Settings, 4 шт. в `/api/issues`) зведено до одного канону `backlog / todo / in-progress / done` у [useWorkflowConfig.js](src/lib/hooks/useWorkflowConfig.js); Settings тепер імпортує звідти.
- **Хибний тост «Налаштування оновлено» при вході в Settings**: авто-збереження тригерилось гідрацією значень із Firestore. Тепер ефекти порівнюють стан із базлайном (останнє відоме збережене значення) і пишуть тільки реальні зміни користувача.
- **Запрошення**: `/api/invitations` тепер реально надсилає імейл (Resend, [src/lib/server/email.js](src/lib/server/email.js)); додано запрошення по посиланню: `POST /api/invitations/link` (токен 256 біт, у БД лише SHA-256-хеш, роль зафіксована, 7 днів, ліміт використань, відкликається) + `/invite/[token]` + блок у обох модалках запрошення.
- **Брендинг**: кнопки «Скасувати»/«Готово» знизу пікера кольору (замість хрестика), біла/брендова підложка під лого на екрані вибору організації, кеш брендингу сайдбару в localStorage — без мигання стандартної теми при перезавантаженні.
- **Аналітика**: сірий фон сторінки + білі картки (загальна аналітика, аналітика проєкту, таб «Команда»; KpiCard тепер білий).

> ⚠️ Для роботи імейлів у продакшні у Vercel мають бути `RESEND_API_KEY`, `EMAIL_FROM` (верифікований домен у Resend) і `NEXT_PUBLIC_APP_URL` (для лінків у листах).

## 🟢 Наступна велика задача — редизайн чату задачі за референсом QuickTeam+
**Рішення власника:** чат у задачі візуально наближаємо до чату QuickTeam+ (референс: `../qt`, а також уже портований [src/components/workspace/qtplus/chat/ChatMessage.jsx](src/components/workspace/qtplus/chat/ChatMessage.jsx)). Фон лишається наш. QuickTeam+ НЕ чіпаємо.

Обсяг: L. Складові:
1. **Візуал** — бульбашки, аватарки, групування повідомлень, красивіший reply із прев'ю цитованого і аватаром (як у QT+).
2. **Read receipts** — `readBy` на повідомленнях + позначки «прочитано» (механіка вже є в qtplus-панелі `usePortalChat` — переносимо патерн у наш чат задачі).
3. **Зберегти нашу перевагу** — редагування повідомлень (у QT+ його нема, не втратити).
4. **Файли в чаті** — прев'ю (лайтбокс), завантаження, видалення: разом із файлом видаляти обʼєкт зі Storage (`deleteObject` за `storagePath`; зберігати `storagePath` у повідомленні при завантаженні, не лише URL).

**Paste-ready промпт для нової сесії:**
```
Прочитай WORK-PLAN.md, розділ «редизайн чату задачі за референсом QuickTeam+».
Зроби редизайн чату в деталях задачі (IssueDetail / таб чату задачі):
візуально за референсом src/components/workspace/qtplus/chat/* і репо ../qt
(аватарки, групування, reply з цитатою, read receipts через readBy),
збережи наше редагування повідомлень, додай перегляд/завантаження/видалення
файлів з чистками у Firebase Storage. Фон чату не міняти. QuickTeam+ не чіпати.
Після реалізації: npm run lint і npm run build.
```

## 💡 Ідеї ШІ-інтерактивності (бек-лог, обговорити пріоритет)
1. **Дзвінок → задачі**: завантажуєш запис дзвінка (аудіо) → транскрипція → Claude API виділяє action items → чернетки задач із виконавцями/дедлайнами, які підтверджуєш одним кліком. Найбільша практична цінність, MVP: сторінка «Імпорт дзвінка» + `/api/ai/call-to-tasks`.
2. **Саммарі**: щоденне/тижневе авто-саммарі по проєкту (що зроблено, що застрягло, хто перевантажений) — у чат проєкту або на дашборд; тригер cron.
3. **ШІ-асистент задачі**: кнопка в задачі — «декомпозуй на підзадачі», «оціни час», «напиши опис з коментарів».
4. **Гейміфікація «офіс»**: інтерактивна мапа-кабінет, де аватари учасників «сидять» у кімнатах-проєктах (presence вже трекається у `presence/{userId}`), статуси/таймер видно на мапі. Вау-ефект для демо, обсяг L.
5. **ШІ-дайджест для клієнта** (через портал QT+): автогенерований статус-репорт проєкту без внутрішньої кухні.

**Paste-ready промпт (MVP №1):**
```
Прочитай WORK-PLAN.md, розділ «Ідеї ШІ-інтерактивності», пункт 1 (дзвінок → задачі).
Зроби MVP: сторінка завантаження аудіо дзвінка, серверний роут з транскрипцією
і викликом Claude API (модель claude-sonnet-5) для виділення action items,
екран підтвердження чернеток задач перед створенням через існуючий /api/issues.
Ключ ANTHROPIC_API_KEY з env. Спитай мене лише якщо бракує доступів.
```
