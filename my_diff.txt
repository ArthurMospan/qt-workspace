Created At: 2026-07-07T12:58:11Z
Completed At: 2026-07-07T12:58:11Z

				The command completed successfully.
				Output:
				commit d4fe09912b34bc6d1d58371e06ed270a9e5e5b1e
Author: ArthurMospan <arthurmospanwork@gmail.com>
Date:   Mon Jul 6 18:08:13 2026 +0300

    @
    refactor: make brand tokens the single source of truth (D-1)
    
    The @theme tokens in globals.css were defined but never referenced —
    product code hardcoded the same 7 brand hexes ~1350 times across 89
    files. Rename the tokens to clean semantic names (ink / ink-hover /
    canvas / surface / line / muted / faint) and codemod every
    value-identical Tailwind arbitrary-hex color class to the matching
    utility. Change a brand value in one place now propagates everywhere.
    
    Zero visual change (each hex maps to a token of the identical value),
    verified by a clean production build. Login and Onboarding left
    untouched per design policy; ui-kit / ui-diff reference pages excluded.
    
    Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
    @

diff --git a/src/app/workspace/[projectId]/page.js b/src/app/workspace/[projectId]/page.js
index 6f1f0a9..3a4b108 100644
--- a/src/app/workspace/[projectId]/page.js
+++ b/src/app/workspace/[projectId]/page.js
@@ -183,7 +183,7 @@ export default function BoardPage({ params }) {
             {isShared && (
               <Link
                 href={`/workspace/${projectId}/portal`}
-                className={`relative flex items-center justify-center gap-[6px] w-[36px] h-[36px] p-0 sm:w-auto sm:h-auto sm:px-[14px] sm:py-[7px] rounded-[10px] text-[12px] font-semibold transition-all whitespace-nowrap bg-[#f4f4f5] text-[#9a9a9a] hover:text-[#6366f1] hover:bg-[#6366f1]/8`}
+                className={`relative flex items-center justify-center gap-[6px] w-[36px] h-[36px] p-0 sm:w-auto sm:h-auto sm:px-[14px] sm:py-[7px] rounded-[10px] text-[12px] font-semibold transition-all whitespace-nowrap bg-canvas text-muted hover:text-[#6366f1] hover:bg-[#6366f1]/8`}
                 title="QuickTeam+"
               >
                 <MessageSquare size={13} />
@@ -282,7 +282,7 @@ export default function BoardPage({ params }) {
       {activeTab === 'board' && (
         loading ? (
           <div className="flex-1 flex items-center justify-center">
-            <div className="w-7 h-7 border-[3px] border-[#e9e9e9] border-t-[#1f1f1f] rounded-full animate-spin" />
+            <div className="w-7 h-7 border-[3px] border-line border-t-[#1f1f1f] rounded-full animate-spin" />
           </div>
         ) : (
           <div className="flex-1 min-h-[500px] flex flex-col">


Created At: 2026-07-07T12:58:17Z
Completed At: 2026-07-07T12:58:17Z

				The command completed successfully.
				Output:
				diff --git a/src/app/workspace/[projectId]/page.js b/src/app/workspace/[projectId]/page.js
index 3a4b108..e744a1c 100644
--- a/src/app/workspace/[projectId]/page.js
+++ b/src/app/workspace/[projectId]/page.js
@@ -42,6 +42,7 @@ export default function BoardPage({ params }) {
 
   // Portal tab visible only when project is shared (synced to QT)
   const isShared = project?.visibility === 'shared';
+  const isArchived = project?.status === 'archived';
 
   const [activeTab, setActiveTab] = useState('board');
   const [boardSprintFilter, setBoardSprintFilter] = useState(() => {
@@ -65,6 +66,9 @@ export default function BoardPage({ params }) {
     return 'all';
   });
 
+  const [analyticsPriorityFilter, setAnalyticsPriorityFilter] = useState('all');
+  const [analyticsTypeFilter, setAnalyticsTypeFilter] = useState('all');
+
   useEffect(() => {
     if (typeof window !== 'undefined') {
       localStorage.setItem(`qt_board_sprint_${projectId}`, boardSprintFilter);
@@ -168,13 +172,18 @@ export default function BoardPage({ params }) {
   const isBoard = activeTab === 'board';
 
   return (
-    <div className={`flex-1 h-full bg-transparent ${isBoard ? 'overflow-hidden' : 'overflow-y-auto overflow-x-hidden custom-scrollbar'}`}>
-      <div className={`w-full flex flex-col gap-2 page-gutter pt-[56px] ${isBoard ? 'h-full pb-[24px]' : 'min-h-full pb-[120px]'}`}>
+    <div className={`flex-1 h-full bg-transparent overflow-hidden flex flex-col`}>
+      <div className={`w-full flex flex-col gap-2 page-gutter pt-[56px] h-full pb-0`}>
 
       {/* ── PageHeader ── */}
       <PageHeader
         variant="main"
-        title={project?.name}
+        title={
+          <div className="flex items-center gap-2">
+            {project?.name} 
+            {isArchived && <span className="text-[10px] uppercase tracking-wider font-bold bg-[#f3f4f6] text-muted px-2 py-1 rounded-md">В архіві</span>}
+          </div>
+        }
         tabs={TABS(projectId)}
         activeTab={activeTab}
         onTabChange={setActiveTab}
@@ -193,7 +202,7 @@ export default function BoardPage({ params }) {
                 )}
               </Link>
             )}
-            {can(orgRole, 'edit:board_columns') && (
+            {!isArchived && can(orgRole, 'edit:board_columns') && (
               <Button
                 onClick={() => setShowConfigModal(true)}
                 icon={Settings2}
@@ -202,16 +211,18 @@ export default function BoardPage({ params }) {
                 title="Налаштування дошки"
               />
             )}
-            <Button
-              onClick={() => setShowCreateTaskModal(true)}
-              style="primary"
-              size="lg"
-              icon={Plus}
-              collapseAt="sm"
-              title="Створити завдання"
-            >
-              Створити завдання
-            </Button>
+            {!isArchived && (
+              <Button
+                onClick={() => setShowCreateTaskModal(true)}
+                style="primary"
+                size="lg"
+                icon={Plus}
+                collapseAt="sm"
+                title="Створити завдання"
+              >
+                Створити завдання
+              </Button>
+            )}
           </>
         }
         filters={
@@ -274,6 +285,33 @@ export default function BoardPage({ params }) {
                   variant="ghost"
                 />
               </FilterBar>
+          ) : activeTab === 'analytics' ? (
+            <FilterBar>
+              <Select
+                variant="ghost"
+                value={analyticsPriorityFilter}
+                onChange={setAnalyticsPriorityFilter}
+                options={[
+                  { value: 'all', label: 'Всі пріоритети' },
+                  { value: 'blocker', label: 'Blocker', dotColor: '#ef4444' },
+                  { value: 'high', label: 'High', dotColor: '#f97316' },
+                  { value: 'medium', label: 'Medium', dotColor: '#eab308' },
+                  { value: 'low', label: 'Low', dotColor: '#9a9a9a' }
+                ]}
+              />
+              <Select
+                variant="ghost"
+                value={analyticsTypeFilter}
+                onChange={setAnalyticsTypeFilter}
+                options={[
+                  { value: 'all', label: 'Всі типи' },
+                  { value: 'epic', label: 'Epic' },
+                  { value: 'feature', label: 'Feature' },
+                  { value: 'task', label: 'Task' },
+                  { value: 'bug', label: 'Bug' }
+                ]}
+              />
+            </FilterBar>
           ) : null
         }
       />
@@ -297,6 +335,7 @@ export default function BoardPage({ params }) {
                 onAddIssue={handleAddIssue}
                 onMoveIssue={handleMoveIssue}
                 issueLinks={issueLinks}
+                isArchived={isArchived}
               />
           </div>
         )
@@ -322,11 +361,17 @@ export default function BoardPage({ params }) {
           members={members}
           project={project}
           projectId={projectId}
+          priorityFilter={analyticsPriorityFilter}
+          typeFilter={analyticsTypeFilter}
         />
       )}
 
       {activeTab === 'team' && (
-        <ProjectTeamTab members={members} />
+        <ProjectTeamTab 
+          members={members} 
+          issues={issues} 
+          projectId={projectId} 
+        />
       )}
       </div>
     </div>


