# Профіль браузера для зйомки: сесія входу в прод, у репозиторії їй не місце.
tools/.profile/
tools/context.json
# Зібрані файли: index.html і product.css генеруються з шаблона й /ui-kit.
# У репозиторії лежить джерело, а не результат — інакше кожна правка
# компонента дає дифи в двох місцях.
dist/
