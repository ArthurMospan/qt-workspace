// src/app/twitter-image.js — the same card, for the clients that ask for
// `twitter:image` instead of `og:image`. One drawing, two conventions.
export { default, alt, size, contentType } from './opengraph-image';
